src = $(wildcard ./src/*.c)
obj = $(patsubst ./src/%.c,./obj/%.o,$(src))
myArgs = $(-Wall -g)


ALL:test.out

test.out:$(obj)
	gcc $^ -o $@ $(myArgs) 

$(obj):./obj/%.o:./src/%.c
	gcc -c $< -o $@ -I./inc $(myArgs)
	
clean:
	-rm test.out $(obj)

.PHONY:clean ALL
